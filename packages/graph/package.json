{
  "name": "@shelfmark/graph",
  "version": "0.0.0",
  "description": "Throttle-correct Microsoft Graph drive client: PKCE OAuth, personal + SharePoint drive resolution, delta queries, pagination, Retry-After discipline",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js", "require": "./dist/index.cjs" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --sourcemap",
    "test": "vitest run"
  },
  "dependencies": { "axios": "^1.7.0" },
  "devDependencies": { "@types/node": "^26.4.0" },
  "engines": { "node": ">=20" }
}
